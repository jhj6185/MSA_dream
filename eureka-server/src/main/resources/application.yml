server:
  port: 8761

spring:
  application:
    name: discovery-service

# 유레카 클라이언트 설정      
eureka:
  client:
    register-with-eureka: false  # 유레카에 등록할지 여부.
    fetch-registry: false # 유레카에서 조회할지 여부
    
